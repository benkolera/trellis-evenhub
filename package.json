{
  "name": "trellis-evenhub",
  "version": "0.1.7",
  "private": true,
  "description": "Trellis on Even Realities G2 — surfaces now / next / focus / habits on the glasses HUD.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "pack": "evenhub pack ./app.json ./dist -o trellis.ehpk",
    "qr": "evenhub qr",
    "simulator": "evenhub-simulator http://localhost:5173"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.10"
  },
  "devDependencies": {
    "@evenrealities/evenhub-simulator": "^0.7.3",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
