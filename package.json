{
  "name": "trellis-evenhub",
  "version": "0.1.0",
  "private": true,
  "description": "Trellis on Even Realities G2 — surfaces now / next / focus / habits on the glasses HUD.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "pack": "evenhub pack",
    "qr": "evenhub qr"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "^0.0.10"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
